# java-calculator-precourse

todo : 
    1. 올바른 사용자 입력(, : 으로 구분된)을 파싱하는 함수 -> 숫자들 리스트로 반환
    2. //와 \n 사이에 오는 문자는 커스텀 구분 문자로 취급
    3. 잘못 된 입력을 했을 경우 Illegal Argument Exception

추가 고려사항
    1. 입력이 "구분자와 양수로 이뤄진 문자열" 이므로 양의 정수가 아니라고 생각, 구분자가 연속으로 오면 오류로 취급?
    2. 자료형에 담을 수 있는 크기가 넘어가면 ?